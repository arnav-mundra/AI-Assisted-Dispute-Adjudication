[pytest]
# Pin the rootdir and put the repository root on sys.path, so `from src...`
# resolves no matter which directory pytest is invoked from. `src/` is an
# implicit namespace package (no top-level __init__.py).
testpaths = tests
pythonpath = .
